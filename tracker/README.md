# Tracker

The Story's project, complete: a tiny issue-tracker CLI born in Chapter 1
(a skeleton and a deadline), hardened in Chapter 2 (a quote character and
atomic writes), and finished in Chapter 12 (v1.0 + the toolkit).

    python3 tracker.py add "rotate the keys" #infra
    python3 tracker.py list
    python3 tracker.py close 1
    python3 tracker.py search infra

Suite: `python3 -m unittest discover -s tests` (10 tests, green).
This directory is the WORKED EXAMPLE — the answer key to the twelve chapters
in ../lessons/story-*.html. Rebuild yours by walking the story; compare here.
